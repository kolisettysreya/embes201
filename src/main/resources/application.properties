spring.application.name=SpringBootSDPProject
server.port=8080

# MySQL connection (Kubernetes service name)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://mysql:3306/event
spring.datasource.username=root
spring.datasource.password=root

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Expose all actuator endpoints
management.endpoints.web.exposure.include=*
management.endpoints.web.base-path=/actuator

# No need to set context path here because Tomcat uses the WAR name
# server.servlet.context-path=/apithis   <-- remove or comment out
